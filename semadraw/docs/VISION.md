# Vision

Graphics should be semantic, deterministic, inspectable, and resolution independent. SemaDraw separates meaning from mechanism and defines a stable drawing model above kernel graphics subsystems and below toolkits.
